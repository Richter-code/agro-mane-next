# Exemplo de variáveis de ambiente (.env.example)
# Copie para .env.local e preencha com valores reais

# Exemplo: API externa (se necessário)
# NEXT_PUBLIC_API_URL=https://api.example.com

# Exemplo: Analytics (se usar)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Exemplo: Chaves privadas (NÃO use NEXT_PUBLIC_ para segredos)
# API_SECRET_KEY=your-secret-here

# Nota: Variáveis com prefixo NEXT_PUBLIC_ são expostas no client-side
# Variáveis sem prefixo ficam apenas no servidor
